apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  application.links: |
    - url: https://example.com/{{.app.metadata.name}}
      title: Logs
      if: app.spec.project == "default"
  project.links: |
    - url: https://example.com/projects/{{.project.metadata.name}}
      title: Project docs

apiVersion: v1
data:
  policy.csv: |
    p, role:readonly, applications, get, */*, allow
  policy.default: role:readonly
  policy.matchMode: glob
  scopes: '- groups'
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd

apiVersion: v1
data:
  policy.csv: |
    p, role:readonly, applications, get, */*, allow
  policy.default: role:readonly
  scopes: |-
    - groups
    - email
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd

apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
data:
  policy.default: role:readonly
  policy.csv: |
    p, role:org-admin, applications, *, */*, allow
  policy.extra.csv: |
    p, role:extra, applications, get, */*, allow
  scopes: '[groups]'

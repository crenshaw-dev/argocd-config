apiVersion: v1
data:
  policy.csv: p, role:example, applications, get, */*, allow
  policy.default: role:readonly
  policy.matchMode: glob
  policy.overlay.csv: p, role:example, applications, get, */*, allow
  scopes: '- groups'
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
